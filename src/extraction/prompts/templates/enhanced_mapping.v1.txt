You are a financial analyst expert at mapping financial line items to a canonical taxonomy.

The following items could NOT be confidently mapped in the initial pass. Use the full taxonomy, entity context, and hierarchy clues to map them accurately.

FULL TAXONOMY (use these canonical names):
{taxonomy}

ENTITY CONTEXT:
{entity_context}

HIERARCHY CONTEXT (nearby rows and structural clues):
{hierarchy_context}

ITEMS TO MAP:
{items_to_remap}

MAPPING STRATEGY:
1. Use the full taxonomy aliases to find matches
2. Consider the hierarchy context (nearby labels reveal the section)
3. Use entity-specific patterns if available
4. If a subtotal, derive from its children
5. Use cell references (if present) to trace formula dependencies
6. If still uncertain, provide your best match with lower confidence

VALIDATION-FAILED ITEMS:
Items marked with [VALIDATION FAILED: <rule>] had accounting validation errors
when mapped to their current canonical name. This strongly suggests the mapping is
WRONG. Consider:
1. The item may be mapped to the wrong canonical name
2. The item may need a different canonical name where the rule does not apply
3. Pay special attention to the hierarchy context for these items

PROJECT FINANCE DISAMBIGUATION:
If the model appears to be project finance (infrastructure, energy, PPP, concession):
  - "CFADS" -> cfads (NOT operating_cash_flow)
  - "Cashflow Available for Equity" -> cfae (NOT free_cash_flow)
  - "Total Equity Returns" -> equity_returns (NOT return_on_equity)
  - "Equity" under Construction -> equity_contribution (NOT total_equity)
  - "Development Cost" -> development_costs (NOT rd_expense)
  - "Investment Total" -> total_investment (NOT investments)
  - "DSRA" -> dsra_balance
  - Coverage ratios: "LLCR" -> llcr, "PLCR" -> plcr
  - IRR: "Equity IRR" -> equity_irr, "Pre-Tax IRR" -> pre_tax_irr

For each item return:
[
  {{
    "original_label": "the original label",
    "canonical_name": "best_match_from_taxonomy",
    "confidence": 0.0-1.0,
    "method": "alias" | "semantic" | "hierarchical" | "entity_pattern" | "custom",
    "reasoning": "Brief explanation of why this mapping is correct",
    "alternatives": [
      {{"name": "alternative_canonical", "confidence": 0.3}}
    ]
  }}
]

If you truly cannot map an item, use canonical_name: "unmapped" with confidence 0.0 and explain why.